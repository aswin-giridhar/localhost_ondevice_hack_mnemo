[pytest]
# Default `pytest` runs only the fast suite. The real end-to-end tests under
# tests/integration need a running Ollama (+ models / camera / TTS) and are run
# explicitly:  PYTHONPATH=src python3 -m pytest tests/integration -v -s
addopts = --ignore=tests/integration
testpaths = tests
